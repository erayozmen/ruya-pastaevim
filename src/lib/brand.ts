/**
 * Path (under /public) of the final logo file. Leave null until the logo
 * exists: Header and Footer then fall back to a text-only brand mark.
 * Favicon: drop `icon.png` / `favicon.ico` into `src/app/` (Next picks it up).
 */
export const BRAND_LOGO_SRC: string | null = null;

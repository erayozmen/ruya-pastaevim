/**
 * Path (under /public) of the final logo file, plus its intrinsic pixel
 * size (required by next/image). Leave BRAND_LOGO_SRC null until the logo
 * exists: Header then falls back to a text-only brand mark.
 * Favicon: drop `icon.png` / `favicon.ico` into `src/app/` (Next picks it up).
 */
export const BRAND_LOGO_SRC: string | null = "/brand/logo.png";
export const BRAND_LOGO_WIDTH = 1942;
export const BRAND_LOGO_HEIGHT = 809;
